# PROGRAM_TREE.md — DutyFlow (Degradation)

> Real file tree last updated 2026-04-07 (calendar refactor; added scheduling_state.json, special_dates.json; implemented main.py).
> Re-run Glob and update this file after any structural change (add/remove/rename files).

---

## File Tree

```
DutyFlow(small)/                          ← Project root
│
├── CLAUDE.md                             ← Claude session guide & golden rules (THIS PROJECT'S LAW)
├── CLAUDE(中文for开发者).md               ← Chinese mirror of CLAUDE.md (for human developer)
├── Requirment.md                         ← Scheduling constraint spec (hard/soft rules, data fields)
├── pyproject.toml                        ← uv project manifest; name="dutyflow", requires-python=">=3.12"
├── uv.lock                               ← Dependency lock file (generated by `uv lock`)
├── .python-version                       ← Pins Python 3.12 for uv auto-selection
├── .env                                  ← D-drive cache path env vars (UV_CACHE_DIR, HF_HOME, etc.)
├── README.md                             ← Stub README (generated by uv init, not yet written)
├── main.py                               ← 编排器：读状态→推算月份→求解→导出
│                                            · 读取 scheduling_state.json 推算下一月
│                                            · 读取 special_dates.json 加载目标月特殊配置
│                                            · 调用 load_teacher_records → DutySolver → export_to_excel
│                                            · 不更新 scheduling_state.json（由 clean_schedule.py 归档时更新）
│
├── poc_loader.py                         ← Phase 1 data loader: reads "清洗后数据" sheet → List[TeacherRecord]
│                                            · Loads all config from PARAMS_REGISTRY.yaml (no hardcoded names)
│                                            · Defines TeacherRecord frozen dataclass
│                                            · load_teacher_records(excel_path) → list[TeacherRecord]
│                                            · __main__ block for manual verification
│
├── poc_solver.py                         ← Phase 3 CP-SAT solver: List[TeacherRecord] → "X月排班（暂定）" sheet
│                                            · build_schedule_days(year, month, special_cfg) → list[ScheduleDay]
│                                              真实日历枚举（节假日排除、调休周六加入、全体班主任日抑制）
│                                            · DutySolver(records, config, target_month, special_dates)
│                                              target_month: "YYYY-MM"; num_weeks 从日历推导（非硬编码）
│                                            · 变量键 (tid, week_idx, day_of_week, slot_id) 不变
│                                            · 6 hard constraints (HC-1~HC-6); soft objective (SC-1~SC-5)
│                                            · export_to_excel() 输出真实日期标签（如"5月6日(周二)"）
│                                            · __main__ block: 读 scheduling_state.json 推算月份后运行
│
├── clean_schedule.py                     ← Standalone data-cleaning script (NOT a runtime module)
│                                            Reads/writes c:/Users/31249/Desktop/晚自修排版.xlsx:
│                                            · Normalizes 楼层 in "次数" sheet（无补录缺失老师逻辑）
│                                            · On re-run: preserves A-G from existing "清洗后数据"
│                                            · Dynamically excludes sheets containing "排班" suffix
│                                            · collect_duty_counts() 兼容旧"星期"列和新"日期"列格式
│                                            · BANZHUREN: 16 members
│                                            Re-run after user renames "X月排班（暂定）" → "X月" to archive.
│
├── scheduling_state.json                 ← 月份状态文件（运行时可变，不纳入git）
│                                            · last_generated: 上次生成的月份 "YYYY-MM"
│                                            · generated_months: 已生成月份列表（审计日志）
│
├── special_dates.json                    ← 每月特殊日期配置（运行前手动编辑）
│                                            · 以 "YYYY-MM" 为键；字段：holiday_ranges / extra_workdays /
│                                              all_bz_required_days / notes
│
├── 临时要求.md                            ← 历史记录，已被 special_dates.json 取代（可删除）
│
├── .dutyflow_meta/                       ← AI-readable project state (English, Claude reads this)
│   ├── ARCHITECTURE.md                   ← System design, module status table, data structures
│   ├── PARAMS_REGISTRY.yaml              ← All numeric constants & config values (single source of truth)
│   ├── TECHNICAL_DEBT_&_TODO.md          ← Active and completed debt/todo items
│   └── PROGRAM_TREE.md                   ← This file — real disk structure with annotations
│
├── .dutyflow_meta（中文for开发者）/        ← Human-readable project state (Chinese, developer reads this)
│   ├── ARCHITECTURE.md                   ← 系统架构、模块状态表（中文镜像）
│   ├── PARAMS_REGISTRY.yaml              ← 参数注册表（中文注释镜像）
│   ├── TECHNICAL_DEBT_&_TODO.md          ← 技术债与待办（中文镜像）
│   └── PROGRAM_TREE.md                   ← 文件树（中文注释镜像）
│
├── .venv/                                ← Local Python venv (CPython 3.12.13 from D-drive GlobalCache)
│   └── Scripts/python.exe               ← Interpreter: D:\...\GlobalCache\uv_python\cpython-3.12.13-...\
│
└── .git/                                 ← Git repository (main branch, initial commit)
```

---

## External Data File (not in repo)

```
c:/Users/31249/Desktop/
└── 晚自修排版.xlsx                        ← Source-of-truth roster & schedule (not gittracked — contains PII)
    ├── sheet: 次数                        ← Teacher roster (cleaned by clean_schedule.py)
    ├── sheet: 清洗后数据                   ← Generated output: roster + historical duty statistics
    ├── sheets: 2025年9月 … 2026年4月       ← Monthly schedule grids (format: YYYY年M月, auto-detected by regex)
    └── sheet: X月排班（暂定）              ← Solver output (excluded from duty-count scan; user renames to "X月")
```

---

## Files NOT YET CREATED (planned)

```
DutyFlow(small)/
├── rules.json                            ← Phase 2: RETIRED — absorbed into PARAMS_REGISTRY.yaml
├── streamlit_app.py                      ← Optional: Streamlit result viewer (post-solver)
│
└── tests/                                ← Unit tests
    ├── test_loader.py                    ← Tests for poc_loader cleaning functions
    └── test_solver.py                    ← Tests for solver constraint satisfaction
```

---

## Update Protocol

When adding or removing files, run the following and paste the output above:

```bash
find "D:/SoftwareCode/MyCyberLab/DutyFlow(small)" \
  -not -path "*/.venv/*" \
  -not -path "*/.git/*" \
  -not -path "*/__pycache__/*" \
  | sort
```
